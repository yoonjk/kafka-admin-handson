---
  clusters:
    cluster-1:
      broker_list:
        - "not-needed-for-rolling-restart:9092"
      zookeeper: "zookeeper1:2181"  # change to your zookeeper IP
  local_config:
    cluster: cluster-1